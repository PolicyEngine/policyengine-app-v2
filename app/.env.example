# =============================================================================
# PolicyEngine App v2 - Frontend Configuration
# =============================================================================
# Copy this to .env for local overrides. Vite auto-loads .env files.

# API URL - where the v2 API is running
# Local development: auto-proxied via Vite config (no .env needed)
# Production: https://v2.api.policyengine.org
VITE_API_V2_URL=http://localhost:8000
