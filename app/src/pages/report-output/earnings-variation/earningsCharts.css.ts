import { style } from '@vanilla-extract/css';

export const chartWrapper = style({
  width: '100%',
  position: 'relative',
});

export const watermark = style({
  display: 'block',
  marginLeft: 'auto',
  opacity: 0.8,
});
