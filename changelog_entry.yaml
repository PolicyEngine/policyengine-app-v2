- bump: patch
  changes:
    added:
      - Add SMF citation on AI and junior hiring, which cites PolicyEngine UK's estimate for extending the employer NI exemption to age 25
      - Add City AM citation on Burnham and junior hiring, which cites PolicyEngine UK's estimate for extending the NI contribution exemption to 21-24-year-old NEETs
      - Add the Introducing PolicyBench research post
      - Add the "How we used Claude Code to apply for the Public Benefit Innovation Fund" research post
      - Add the "Expansion of free childcare hours" UK research post covering the 2023 extended childcare entitlement reform
      - Add UK Universal Credit rebalancing analysis dashboard at /uk/uc-rebalancing
      - Add UK fuel duty rise cancellation analysis dashboard at /uk/cancelling-fuel-duty-rise
      - Redirect the /policybench and /:countryId/policybench vanity paths to policybench.org
      - Proxy the AI beliefs research dashboard (elicited LLM beliefs about economic elasticities across 17 models) at /ai-beliefs
      - Add the NJ Child Tax Credit increase calculator zone route at /us/nj-ctc-increase
      - Publish the NJ Child Tax Credit increase calculator on /us/research with cover image and authorship
      - Add the NICs exemption for recently-inactive employees dashboard at /uk/nics-exemption-inactive-employees
      - Add the temporary VAT cut on domestic electricity dashboard at /uk/electricity-vat-cut
    changed:
      - Move the Claude plugin page to /:countryId/ai-agents (runtime-agnostic — the skills catalog serves Claude Code and Codex) with a permanent redirect from /:countryId/claude-plugin; refresh the hero eyebrow and add the Codex install path
      - Correct the Claude plugin page stats (they were frozen at 24 skills / 21 agents / 4 commands / 7 bundles) and read them from a checked-in pluginStats.json regenerated from policyengine-skills, so the page tracks the rebuilt catalog
      - Move the bill tracker to /us/bill-tracker (canonical URL); /us/state-legislative-tracker now permanently redirects there
      - Stop mirroring policy, household, and region writes to the deprecated API v2 alpha service; the app now uses the v1 API exclusively
      - Proxy UK Python package docs at /uk/python and UK API docs at /uk/api so the country-parameterized "Python" and "API" nav links work on UK pages
      - Align the calculator app header (e.g. /us/reports) with the main website header — hover-open dropdowns, per-item underline, Model dropdown with sub-items, Python link, and Events under About
    fixed:
      - Point "Reproduce in Python" snippets for a US national run at the certified populace-us dataset instead of the deprecated policyengine-us-data repo
      - Add a callout on society-wide results explaining why every output reads "No change" when a reform changes nothing in effect in the simulated year (for example a credit that has sunset), so users can tell a genuine no-op from a start-date or sunset mismatch
      - Declare the image optimiser width allowlist in website/next.config.ts (the vercel.json images block is ignored for Next.js projects), fixing team headshots and supporter logos that returned 400 on retina displays because their 2x srcSet width of 512 was not allowed
