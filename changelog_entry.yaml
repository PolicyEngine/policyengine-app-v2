- Name states in the parameter search filter, explain the contributed filter, and stop long parameter values from squeezing result labels
- Lead the build page with search, fold the policy tree behind a toggle, and open a search result's folder in the tree
- Let the draft reform panel fold away
- Add the two-part UK childcare reform dashboard to Research
- Dock the draft reform and report adjust panels as a full-height right-hand plane that folds to a spine
- Trace the parameter to variable dependency map from the model so validation matches a reform to the variables it actually moves
- Show how well the microdata is calibrated for the variables a reform moves, next to the scorecard context on the report's validation tab
- Pin what each report's validation was matched against (populace release, model version, per-variable fit) and say when a newer release has made that comparison historical
- Give flagship report links a durable id so a shared link opens with its provisions and validation on any device
- Link the validation tab's calibration rows to the dashboard filtered by source and level, and the scorecard rows to their external source and the scorecard's comparison and method pages
- Replace the report's duplicated progress bars and skeleton blocks with one status card while the run computes, and explain local-only links opened elsewhere
- Wait for every simulation to load before starting a report's calculation, so a report is never scored as current law against itself and stored as "no change"
- Stop the report page logging "Query data cannot be undefined" on every fresh load
- Show one loading view while a report computes: the reform it scores, the run's stages as they happen, and the data checks as they arrive
- Keep a flagship report's validation off the screen until the run has finished; the checks still start with the run so they are ready when it completes
- Regenerate the traced dependency map at policyengine-us 1.824.8 so validation matches against the current model
- Point the calibration dashboard links at the dashboard's renamed microcosm path; the old populace path now redirects to a 404
- Say what a calibration row's distance from the reform means in words instead of counting hops
- Use sentence case consistently across interface copy
