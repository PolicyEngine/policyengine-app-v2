# Calculator app URL — used by "Enter PolicyEngine" CTA and other cross-app links.
# In production, this defaults to https://app.policyengine.org
# For local dev, set to the calculator's local URL:
NEXT_PUBLIC_CALCULATOR_URL=http://localhost:3001
